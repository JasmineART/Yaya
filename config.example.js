// Copy to config.js and fill with your keys
window.YAYA_CONFIG = {
  // Supabase: free tier can be used to store newsletter, comments, orders. Create a table 'newsletter', 'comments', 'orders'.
  supabaseUrl: '',
  supabaseAnonKey: ''
};
