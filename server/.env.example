# Copy to .env and fill
# Stripe Configuration (REQUIRED)
STRIPE_SECRET_KEY=sk_live_51SM7yMRMDdiM5E9A...
STRIPE_WEBHOOK_SECRET=whsec_...

# Server Configuration
PORT=4242
NODE_ENV=production

# PayPal (Optional - if using PayPal payments)
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_SECRET=your-paypal-secret

# Database (Optional - for order storage)
SUPABASE_URL=https://xyzcompany.supabase.co
SUPABASE_SERVICE_ROLE=service_role_key_here

# Email Configuration (Optional - for order confirmations)
# Option 1: SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=send@example.com
SMTP_PASS=supersecret

# Option 2: SendGrid API (instead of SMTP)
SENDGRID_API_KEY=

# Security (Optional)
RECAPTCHA_SECRET=

# Render.com Deployment Notes:
# - Set STRIPE_SECRET_KEY in Render dashboard environment variables
# - Root Directory: server
# - Build Command: npm install
# - Start Command: node index.js
# - Outbound IPs: See RENDER_NETWORK_CONFIG.md for IP allowlisting

